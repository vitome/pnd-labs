One router with two lan, both with 2 pcs. The assignment is: to
configure the topology to use staic IPv6 addresses. 

You have to provide static GUA addresses to the machines in the topology.

- lan1 and lan2 must have the subnet 2001:DB8:0:1::/64 and
  2001:DB8:0:2::/64 respectively

- pc1, pc2, pc3 and pc4 must have in the host part 101, 102, 103 and
  104 respectively.

- the router has always 1 in the host part of its own address.
